import { number, date } from '../utils/formatting';
import { Icon } from './Icon';

export function ReportPreview({ preview, type = 'Academic summary' }) {
  return <article className="report-paper"><div className="report-letterhead"><strong>CGPA<span>+</span></strong><div><span>UniPort</span><small>INDEPENDENT ACADEMIC PLANNING</small></div></div><div className="report-heading"><span>FOR PERSONAL REFERENCE</span><h2>CGPA+ Academic Report</h2><p>{type}</p></div><div className="report-meta"><div><span>Student</span><strong>{preview?.studentName || 'Not loaded'}</strong></div><div><span>University</span><strong>University of Port Harcourt</strong></div><div><span>Academic session</span><strong>{preview?.sessionName || '--'}</strong></div><div><span>Generated</span><strong>{date(preview?.createdAt)}</strong></div></div><div className="report-summary"><div><span>CGPA</span><strong>{number(preview?.summary?.cgpa)}</strong></div><div><span>Credit units</span><strong>{number(preview?.summary?.totalCredits, 0)}</strong></div><div><span>Quality points</span><strong>{number(preview?.summary?.qualityPoints)}</strong></div></div><div className="table-scroll"><table><thead><tr><th>Course</th><th>Units</th><th>Grade</th><th>Quality points</th></tr></thead><tbody>{preview?.courses?.length ? preview.courses.map(c => <tr key={c.id || c.code}><td>{c.code}</td><td>{c.credits}</td><td>{c.grade}</td><td>{c.qualityPoints}</td></tr>) : <tr><td colSpan={4}>Academic report data has not been loaded.</td></tr>}</tbody></table></div><div className="report-disclaimer"><Icon name="shield" size={16} /><p>This is a CGPA+ Academic Report, not an official University of Port Harcourt transcript. It does not certify results, clearance or graduation eligibility.</p></div><footer>{preview?.id ? `Report reference: ${preview.id}` : 'LAYOUT PREVIEW ONLY. NO VERIFIED REPORT HAS BEEN GENERATED.'}</footer></article>;
}