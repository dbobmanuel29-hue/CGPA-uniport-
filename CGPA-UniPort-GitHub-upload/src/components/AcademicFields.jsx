import { AsyncSelect, Field, Input, UniversityLock } from './ui';
import { academicService } from '../services/academic-service';
export const emptyAcademic = { facultyId: '', departmentId: '', programmeId: '', academicVersionId: '', admissionSessionId: '', currentLevelId: '', currentSessionId: '', currentSemesterId: '', matriculationNumber: '', phone: '' };
export function AcademicFields({ form, setForm, withUniversity = true, optional = true }) {
  const change = key => e => setForm(prev => ({ ...prev, [key]: e.target.value, ...(key === 'facultyId' ? { departmentId: '', programmeId: '', academicVersionId: '', currentLevelId: '', currentSemesterId: '' } : key === 'departmentId' ? { programmeId: '', academicVersionId: '', currentLevelId: '', currentSemesterId: '' } : key === 'programmeId' ? { academicVersionId: '', currentLevelId: '', currentSemesterId: '' } : {}) }));
  return <>{withUniversity && <UniversityLock />}<div className="form-grid"><AsyncSelect label="Faculty" loader={() => academicService.getFaculties()} value={form.facultyId} onChange={change('facultyId')} /><AsyncSelect label="Department" loader={() => academicService.getDepartments({ facultyId: form.facultyId })} dependencies={[form.facultyId]} enabled={!!form.facultyId} value={form.departmentId} onChange={change('departmentId')} /><div className="full-width"><AsyncSelect label="Programme" loader={() => academicService.getProgrammes({ departmentId: form.departmentId })} dependencies={[form.departmentId]} enabled={!!form.departmentId} value={form.programmeId} onChange={change('programmeId')} /></div><AsyncSelect label="Admission session" loader={() => academicService.getAcademicSessions()} value={form.admissionSessionId} onChange={change('admissionSessionId')} /><AsyncSelect label="Current level" loader={() => academicService.getLevels({ programmeId: form.programmeId })} dependencies={[form.programmeId]} enabled={!!form.programmeId} value={form.currentLevelId} onChange={change('currentLevelId')} /><AsyncSelect label="Current academic session" loader={() => academicService.getAcademicSessions()} value={form.currentSessionId} onChange={change('currentSessionId')} /><AsyncSelect label="Current semester" loader={() => academicService.getSemesters({ programmeId: form.programmeId })} dependencies={[form.programmeId]} enabled={!!form.programmeId} value={form.currentSemesterId} onChange={change('currentSemesterId')} />{optional && <><Field label="Matriculation number" optional><Input value={form.matriculationNumber || ''} onChange={change('matriculationNumber')} placeholder="As shown on your student record" /></Field><Field label="Phone number" optional><Input type="tel" autoComplete="tel" value={form.phone || ''} onChange={change('phone')} placeholder="Your contact number" /></Field></>}</div></>;
}